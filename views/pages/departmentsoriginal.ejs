<%- include('../partials/head'); %>
<body>
    <%- include('../partials/header'); %>
    <div class="container-fluid">
    <section>
        <div class="page-header">
            <h1 class="text-dark">Departments</h1>
        </div>
    </section>

         <!-- Click on Modal Button -->
         <section class="section-department">
            <button type="button" class="btn btn-sample mx-auto text-light btn-design" data-bs-toggle="modal" data-bs-target="#department_id">
                Add Department
            </button>
            <div class="modal fade" id="department_id" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                        <div class="modal-header text-light">
                            <h3 class="modal-title" id="exampleModalLabel">New Department</h3>
                            <button type="button" class="btn-close text-light" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <form action="/add_departments" method="post">
                                <div class="row">
                                    <label class="form-label fw-bold">Add Department</label>
                                    <div class="d-flex">
                                        <div class="mb-3 col-md-8 col-sm-12">
                                            <input type="text" class="form-control" id="department_name" name="department_name" placeholder="Department name" required/>
                                        </div>
                                        <div class="col-md-3 col-sm-5 d-flex mx-3">
                                            <input type="submit" value="Add" class="btn btn-sample btn-block text-light text-uppercase mb-3">
                                        </div>
    <!-- Click on Modal Button -->
    <section class="section-department my-3">
        <button type="button" class="btn btn-sample mx-auto text-light btn-design my-3" data-bs-toggle="modal" data-bs-target="#addDepartmentModal">
            Add Department
        </button>

        <!-- Add Department Modal -->
        <div class="modal fade" id="addDepartmentModal" tabindex="-1" aria-labelledby="addDepartmentModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg">
                <div class="modal-content">
                    <div class="modal-header text-light">
                        <h3 class="modal-title" id="addDepartmentModalLabel">New Department</h3>
                        <button type="button" class="btn-close text-light" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <form action="/add_departments" method="post">
                            <div class="row">
                                <label class="form-label fw-bold">Add Department</label>
                                <div class="d-flex">
                                    <div class="mb-3 col-md-8 col-sm-12">
                                        <input type="text" class="form-control" id="department_name" name="department_name" placeholder="Department name" required/>
                                    </div>
                                    <div class="col-md-3 col-sm-5 d-flex mx-3">
                                        <input type="submit" value="Add" class="btn btn-sample btn-block text-light text-uppercase mb-3">
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>


       

       <!--    TABLE FOR DEPARTMENTS LIST   -->
       <section class="section-table mt-3">
            <table id="example" class="table table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>S No.</th>
                        <th>Deparment Name</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (departmentData && departmentData.length !== 0) { %>
                        <% departmentData.forEach(function(data, index) { %>
    <!-- TABLE FOR DEPARTMENTS LIST -->
    <section class="section-table my-4">
        <table id="example" class="table table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>S No.</th>
                    <th>Department Name</th>
                    <th>Delete</th>
                    <th>Update</th>
                </tr>
            </thead>
            <tbody>
                <% if (departmentData && departmentData.length !== 0) { %>
                    <% departmentData.forEach(function(data, index) { %>
                        <tr>
                            <td><%= index + 1 %></td>
                            <td><%= data.department_name %></td>
                            <td>
                                <button onclick="return confirmdelete()" type="submit" aria-label="Delete" class="btn btn-danger">Delete</button>
                                <button type="Submit" aria-label="Update" class="btn btn-warning"> Update </button>
                            </td>

                            </form>
                     
                                <form action="/delete_department" method="post" onsubmit="return confirm('Are you sure you want to delete?')">
                                    <input type="hidden" name="department_id" value="<%= data.id %>">
                                    <button type="submit" class="btn btn-danger" aria-label="Delete">Delete</button>
                                </form>
                            </td>
                            <td>
                                <form action="/updateDepartments" method="post" onsubmit="return confirm('Are you sure you want to update?')">
                                    <input type="hidden" name="department_id" value="<%= data.id %>">
                                    <button type="submit" class="btn btn-primary" aria-label="Update">Update</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4">No Data Found</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </section>


    <!-- </section> -->
</div>

    <script>
        function confirmdelete() {
            // Display a confirmation dialog
            let userConfirmed = confirm("Are you sure you want to delete?");
            
            // If the user confirms, submit the form for deletion
            return userConfirmed;
        }
    </script>

    
    <%- include('../partials/footer'); %>
</body>
</html>
